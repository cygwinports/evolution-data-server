--- origsrc/evolution-data-server-2.24.4.1/calendar/libical/src/libical/icaltz-util.c	2009-01-19 21:28:34.000000000 -0600
+++ src/evolution-data-server-2.24.4.1/calendar/libical/src/libical/icaltz-util.c	2009-02-03 15:14:09.003750000 -0600
@@ -28,7 +28,7 @@
 #define LITTLE_ENDIAN 'l'
 #define BYTE_ORDER LITTLE_ENDIAN
 #define bswap32(val) ((((val) & 0x000000ffU) << 24) | (((val) & 0x0000ff00U) <<  8) | (((val) & 0x00ff0000U) >>  8) | (((val) & 0xff000000U) >> 24))
-#elif defined(__linux__)
+#elif defined(__linux__) || defined(__CYGWIN__)
 #include <byteswap.h>
 #include <endian.h>
 #else
@@ -96,7 +96,7 @@ decode (const void *ptr)
     if ((BYTE_ORDER == BIG_ENDIAN) && sizeof (int) == 4)
         return *(const int *) ptr;
     else if (BYTE_ORDER == LITTLE_ENDIAN && sizeof (int) == 4)
-#ifdef __linux__
+#if defined(__linux__) || defined(__CYGWIN__)
         return bswap_32 (*(const int *) ptr);
 #else
          return bswap32 (*(const uint32_t *) ptr);
